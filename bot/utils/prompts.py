from __future__ import annotations

# ──────────────────────────────────────────
# Промпт для парсинга напоминаний
# ──────────────────────────────────────────

REMINDER_PARSE_PROMPT = """Ты помощник по созданию напоминаний. Извлеки параметры напоминания из текста пользователя.

Текущее время пользователя: {current_time}
Часовой пояс: {timezone}

Текст пользователя: {user_text}

Верни JSON с полями:
- "text": текст напоминания (что напомнить)
- "prompt": промпт для Claude (если нужна генерация, иначе null)
- "due_at": время в ISO 8601 UTC (первое срабатывание)
- "is_chain": true/false (повторяющееся или нет)
- "interval_seconds": интервал повтора в секундах (для цепочек, иначе null)
- "steps_left": количество повторений (null = без ограничения)
- "end_at": дата окончания в ISO 8601 UTC (null = бессрочно)
- "silent": 1 если нужен тихий режим (ночь/утро), иначе 0

Примеры:
- "напомни через 30 минут купить хлеб" → is_chain=false, text="купить хлеб"
- "каждый понедельник в 9 утра план недели, 4 раза" → is_chain=true, steps_left=4
- "каждый день в 22:00 пора спать, тихо" → is_chain=true, silent=1

Верни ТОЛЬКО валидный JSON без пояснений."""

# ──────────────────────────────────────────
# Промпт для определения часового пояса
# ──────────────────────────────────────────

TIMEZONE_DETECT_PROMPT = """Проанализируй последние сообщения пользователя и определи его часовой пояс.
Ищи упоминания города, страны, времени суток, рабочего времени.

Сообщения:
{messages}

Если удалось определить — верни JSON: {{"timezone": "Europe/Moscow"}}
Если нет — верни JSON: {{"timezone": null}}

Верни ТОЛЬКО JSON без пояснений."""
